import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aikicode.studio',
  appName: 'studio',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
